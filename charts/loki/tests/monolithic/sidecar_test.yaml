# $schema: https://raw.githubusercontent.com/helm-unittest/helm-unittest/refs/heads/main/schema/helm-testsuite.json
suite: monolithic sidecar
templates:
  - monolithic/statefulset.yaml
  - config.yaml

set:
  deploymentMode: Monolithic
  loki.useTestSchema: true
  loki.storage.bucketNames.chunks: chunks
  loki.storage.bucketNames.ruler: ruler
  loki.storage.bucketNames.admin: admin
  singleBinary.sidecar: true
  loki.image.tag: 0.0.0
  sidecar.image.tag: 0.0.0

tests:
  - it: should render the ruler rules sidecar
    asserts:
      - hasDocuments:
          count: 1
        template: monolithic/statefulset.yaml
      - isKind:
          of: StatefulSet
        template: monolithic/statefulset.yaml
      - template:
          monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers
          content:
            name: loki-sc-rules
            image: docker.io/kiwigrid/k8s-sidecar:0.0.0
          any: true
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: METHOD
            value: WATCH
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: LABEL
            value: loki_rule
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: FOLDER
            value: /rules
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: RESOURCE
            value: both
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.volumes
          content:
            name: sc-rules-volume
            emptyDir: {}
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: sc-rules-volume
            mountPath: /rules

  - it: should render optional sidecar env vars and a sized shared volume when configured
    set:
      sidecar.enableUniqueFilenames: true
      sidecar.skipTlsVerify: true
      sidecar.disableX509StrictVerification: true
      sidecar.rules.sizeLimit: 1Gi
      sidecar.rules.watchMethod: SLEEP
      sidecar.rules.label: custom_rule
      sidecar.rules.labelValue: tenant-a
      sidecar.rules.folder: /tmp/rules
      sidecar.rules.folderAnnotation: loki-folder
      sidecar.rules.resource: configmap
      sidecar.rules.script: /reload.sh
      sidecar.rules.watchServerTimeout: 120
      sidecar.rules.watchClientTimeout: 130
      sidecar.rules.logLevel: DEBUG
    asserts:
      - template: monolithic/statefulset.yaml
        hasDocuments:
          count: 1
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: METHOD
            value: SLEEP
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: LABEL
            value: custom_rule
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: LABEL_VALUE
            value: tenant-a
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: FOLDER
            value: /tmp/rules
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: FOLDER_ANNOTATION
            value: loki-folder
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: RESOURCE
            value: configmap
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: UNIQUE_FILENAMES
            value: "true"
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: SKIP_TLS_VERIFY
            value: "true"
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: DISABLE_X509_STRICT_VERIFICATION
            value: "true"
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: SCRIPT
            value: /reload.sh
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: WATCH_SERVER_TIMEOUT
            value: "120"
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: WATCH_CLIENT_TIMEOUT
            value: "130"
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[1].env
          content:
            name: LOG_LEVEL
            value: DEBUG
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.volumes
          content:
            name: sc-rules-volume
            emptyDir:
              sizeLimit: 1Gi
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: sc-rules-volume
            mountPath: /tmp/rules

  - it: should not add the sidecar container or shared volume when disabled via sidecar.rules.enabled
    set:
      sidecar.rules.enabled: false
    asserts:
      - template: monolithic/statefulset.yaml
        hasDocuments:
          count: 1
      - template: monolithic/statefulset.yaml
        isKind:
          of: StatefulSet
      - notContains:
          template: monolithic/statefulset.yaml
          path: spec.template.spec.containers
          content:
            name: loki-sc-rules
      - template: monolithic/statefulset.yaml
        notContains:
          path: spec.template.spec.volumes
          content:
            name: sc-rules-volume
      - template: monolithic/statefulset.yaml
        notContains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: sc-rules-volume

  - it: should not add the sidecar container or shared volume when disabled via backend.sidecar
    set:
      singleBinary.sidecar: false
    asserts:
      - template: monolithic/statefulset.yaml
        hasDocuments:
          count: 1
      - template: monolithic/statefulset.yaml
        isKind:
          of: StatefulSet
      - notContains:
          template: monolithic/statefulset.yaml
          path: spec.template.spec.containers
          content:
            name: loki-sc-rules
      - template: monolithic/statefulset.yaml
        notContains:
          path: spec.template.spec.volumes
          content:
            name: sc-rules-volume
      - template: monolithic/statefulset.yaml
        notContains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: sc-rules-volume

  - it: should preserve extra containers alongside the generated sidecar
    set:
      singleBinary:
        extraContainers:
          - name: extra-helper
            image: busybox:1.36
            command:
              - sh
              - -c
              - sleep 3600
    asserts:
      - template: monolithic/statefulset.yaml
        hasDocuments:
          count: 1
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers
          content:
            name: extra-helper
            image: busybox:1.36
          any: true
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers
          content:
            name: loki-sc-rules
          any: true

  - it: should use the ruler component image override without changing the sidecar image
    set:
      singleBinary.image.registry: quay.io
      singleBinary.image.repository: custom/loki
      singleBinary.image.tag: 1.2.3
    asserts:
      - template: monolithic/statefulset.yaml
        hasDocuments:
          count: 1
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers
          content:
            name: loki
            image: quay.io/custom/loki:1.2.3
          any: true
      - template: monolithic/statefulset.yaml
        contains:
          path: spec.template.spec.containers
          content:
            name: loki-sc-rules
            image: docker.io/kiwigrid/k8s-sidecar:0.0.0
          any: true
