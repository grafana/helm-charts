{{- if .Values.monitoring.alerts.enabled }}
{{- with .Values.monitoring.alerts }}
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  labels:
    {{- include "loki.labels" $ | nindent 4 }}
    {{- with .labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  name: {{ include "loki.fullname" $ }}-alerts
  namespace: {{ .namespace | default $.Release.Namespace }}
spec:
  {{- $.Files.Get "src/alerts.yaml" | fromYaml | toYaml | nindent 2 }}
{{- end }}
{{- end }}
