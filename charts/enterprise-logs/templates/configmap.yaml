{{- if not .Values.gel.existingSecretForConfig -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gel.fullname" . }}
  labels:
    {{- include "gel.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- tpl .Values.lokidistributed.loki.config . | nindent 4 }}
{{- end -}}
