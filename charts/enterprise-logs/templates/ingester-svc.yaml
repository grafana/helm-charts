apiVersion: v1
kind: Service
metadata:
  name: {{ template "enterprise-logs.fullname" . }}-ingester
  labels:
    app: {{ template "enterprise-logs.name" . }}-ingester
    chart: {{ template "enterprise-logs.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    {{- with .Values.ingester.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- toYaml .Values.ingester.annotations | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.config.server.http_listen_port }}
      protocol: TCP
      name: http-metrics
      targetPort: http-metrics
    - port: {{ .Values.config.server.grpc_listen_port }}
      protocol: TCP
      name: grpc
      targetPort: grpc
  selector:
    app: {{ template "enterprise-logs.name" . }}-ingester
    release: {{ .Release.Name }}
