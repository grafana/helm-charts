storage:
  trace:
    block:
      version: vParquet3
      dedicated_columns:
        - name: cluster
          type: string
          scope: resource
        - name: service.name
          type: string
          scope: resource
